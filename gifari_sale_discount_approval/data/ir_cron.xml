<?xml version="1.0" encoding="utf-8"?>
<odoo>
<data noupdate="1">

    <!-- Cron: SLA Approval Check (setiap jam) -->
    <record id="ir_cron_check_approval_sla" model="ir.cron">
        <field name="name">Sale Discount: Check Approval SLA</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="state">code</field>
        <field name="code">model._cron_check_approval_sla()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">hours</field>
        <field name="active">True</field>
    </record>

</data>
</odoo>
