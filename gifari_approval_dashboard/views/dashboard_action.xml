<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Dashboard Client Action -->
    <record id="action_approval_dashboard" model="ir.actions.client">
        <field name="name">Approval Dashboard</field>
        <field name="tag">gifari_approval_dashboard</field>
    </record>

    <!-- Top-level menu under Sales -->
    <menuitem
        id="menu_approval_dashboard"
        name="Approval Dashboard"
        parent="sale.sale_menu_root"
        action="action_approval_dashboard"
        sequence="1"
        groups="gifari_sale_discount_approval.group_sale_approval_manager,gifari_purchase_approval.group_purchase_approval_manager"/>

</odoo>
